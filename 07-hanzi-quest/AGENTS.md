# Prototype 07 — Hanzi Quest

Read the repository's root AGENTS.md first. This prototype is a standalone static folder: no installation, API key, server, live AI requests or build step. Preserve the relative local asset paths for GitHub Pages subdirectory hosting.

- Keep app requests same-origin. Do not add external fonts, analytics, microphone access or live speech generation.
- Keep XP, word results and settings local. Never commit student data, private deployment metadata, credentials, logs, local absolute paths or source-project history.
- The default voice is packaged complete-word MP3 audio. Changing vocabulary requires a matching recording and matching pinyin.
- Preserve three-mode rotation, equal-length distractors, correction/revisit behavior, reward pauses, XP idempotency and user progress.
- Keep this numbered folder self-contained. Root index/README link to it.
- Respect the user's no-file-deletion constraint.
